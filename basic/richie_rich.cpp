#include <iostream>

using namespace std;

int main() {

    // A current billion
    // X raise billion per year
    // B need to be
    //

    int tt;
    cin >> tt;
    while(tt--) {
        int A, B, X;
        cin >> A >> B >> X;

        // A -> B, with X
        cout << (B - A) / X << endl;

        
    }

}